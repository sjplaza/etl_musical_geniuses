CREATE TABLE music_data
(
    acousticness INT,
    artists VARCHAR,
    danceability VARCHAR,
    duration_ms INT,
    energy DECIMAL,
    explicit INT,
    id VARCHAR,
    instrument DECIMAL,
    key INT,
    liveness DECIMAL,
    loudness DECIMAL,
    mode INT,
    name INT,
    popularity INT,
    release_date DATE,
    speechiness DECIMAL,
    tempo DECIMAL,
    valence DECIMAL,
    year INT,
);

    SELECT *
    FROM music_data;